
class Arena_C:

    # Every dimension will be offset by the offset_val
    OFFSET = 10
    X = 40 + 2 * OFFSET
    Y = 40 + 2 * OFFSET

    OBS_LENGTH = 2.0   # obstacle length on algo
    SCALE = 2   # 2 units in sim = 1 unit on Android sim